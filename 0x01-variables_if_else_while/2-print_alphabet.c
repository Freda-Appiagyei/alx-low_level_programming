#include <stdio.h>

/**
 *main - Every
 *
 *Return: Always
 */
int main(void)
{
	int ch;
	int space;

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
	
	space = '\n';
      	putchar(space);
	return (0);
}
