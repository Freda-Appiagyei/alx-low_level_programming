#include <stdio.h>

/**
 *main - F
 *
 *Return: Fh
 */
int main(void)
{
	int ch;
	int space;

	for (ch = 0; ch < 9; ch++)
		printf("%d, ", ch);
	space = '\n';
	printf("9");
	putchar(space);
	return (0);
}
