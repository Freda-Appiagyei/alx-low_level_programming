#include "main.h"

/**
 *main - check code
 *
 *Return: Always 0
 */
void print_times_table(int n)
{
	int i = n;
	int j;
	int k;

	if (n > 15 || n < 0)
	{
		for (i = 0; i <= n; i++)
		{
		k = 1
		for (j = 0; j <= n; j++)
		{
			k = i * j;
		}
		}
	}
printf("%d, \n", k);
}
